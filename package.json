{
  "devDependencies": {
    "vitepress": "^1.6.3"
  },
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build --base /Resume/ --outDir ./dist",
    "docs:preview": "vitepress preview"
  }
}
